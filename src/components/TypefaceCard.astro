---
import ImageSlideshow from './ImageSlideshow.astro';

interface CardImage {
  src: string;
  alt?: string;
}

interface Props {
  name: string;
  category: string;
  styles: number;
  slug: string;
  defaultImage: CardImage;
  hoverImages: CardImage[];
}

const { name, slug, defaultImage, hoverImages, styles } = Astro.props;
const hasHoverSlideshow = hoverImages.length > 1;
---

<article class="rounded-lg overflow-hidden h-full w-full flex flex-col bg-white">
  <a href={`/typefaces/${slug}`} class="flex flex-col h-full min-h-0 group">
    <div
      class="typeface-card__media relative w-full aspect-[4/3] overflow-hidden"
      data-typeface-card-media
    >
      <img
        src={defaultImage.src}
        alt={defaultImage.alt ?? name}
        width={1600}
        height={1200}
        class="absolute inset-0 w-full h-full object-cover"
        loading="lazy"
        decoding="async"
      />
      {hasHoverSlideshow && (
        <div
          class="typeface-card__hover-layer absolute inset-0 opacity-0 pointer-events-none"
          data-typeface-hover-layer
        >
          <ImageSlideshow
            items={hoverImages.map((img) => ({ src: img.src, alt: img.alt ?? name }))}
            objectFit="cover"
            stageClass="h-full w-full"
            hoverOnly
          />
        </div>
      )}
    </div>
    <div class="shrink-0 bg-white px-6 py-4 flex justify-between items-baseline gap-4">
      <h3 class="text-base font-normal text-black group-hover:text-haus-red transition-colors truncate">
        {name}
      </h3>
      <span class="text-base text-[#999999] shrink-0">
        {styles} {styles === 1 ? 'style' : 'styles'}
      </span>
    </div>
  </a>
</article>

<style>
  .typeface-card__hover-layer.is-active {
    opacity: 1;
    pointer-events: auto;
  }
</style>
